import {defineProps} from '@vue/runtime-core';
export default defineComponent({
    setup() {
        return () => (
            <div>
                Page text
            </div>
        )
    },
})
